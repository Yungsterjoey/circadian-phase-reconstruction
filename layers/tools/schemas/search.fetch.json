{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "search.fetch",
  "description": "Fetch a single URL and return its cleaned text content. HTML is stripped; one redirect max; https preferred.",
  "type": "object",
  "required": ["url"],
  "additionalProperties": false,
  "properties": {
    "url":         { "type": "string", "format": "uri", "maxLength": 2048, "description": "Absolute http(s) URL." },
    "max_bytes":   { "type": "integer", "minimum": 1024, "maximum": 524288, "default": 65536, "description": "Cap on response body bytes (default 64 KB)." },
    "strip_html":  { "type": "boolean", "default": true, "description": "If true, strip HTML tags and collapse whitespace." },
    "timeout_ms":  { "type": "integer", "minimum": 500, "maximum": 10000, "default": 5000 }
  }
}
