{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tools.list",
  "description": "List every tool currently available to this session (static + any dynamic tools created this session).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "prefix":        { "type": "string", "maxLength": 64, "description": "Optional namespace filter, e.g. 'vfs.' or 'search.'." },
    "include_schema":{ "type": "boolean", "default": false, "description": "If true, embed the full input JSON schema per tool (verbose)." },
    "include_dynamic": { "type": "boolean", "default": true, "description": "If false, only list built-in tools." }
  }
}
